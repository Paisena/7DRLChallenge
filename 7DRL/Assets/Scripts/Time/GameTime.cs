using UnityEngine;
// This scripts holds the information and functions for time and it's progress duringthe training stage
public class GameTime : MonoBehaviour
{
    public int CurrentTime; // Not a set unit of time because it will change over the stages, will have to make a function which translates this number to the correct unit base on the stage
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
